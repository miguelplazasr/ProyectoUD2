{% extends 'layout.html.twig'%}

{% block body -%}
    {{ parent() }}
    <div class="row">
        <div class="col-sm-3">
            <div class="sidebar-nav">
                <div class="navbar navbar-default red" role="navigation">
                    <div class="navbar-header red-nav">
                        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <span class="visible-xs navbar-brand">Menu de Carnetizacion</span>
                    </div>
                    <div class="navbar-collapse collapse sidebar-navbar-collapse">
                        <ul class="nav navbar-nav">
                            <li class="active reda"><a href="#">Menu</a></li>
                            <li class="redli"><a href="{{ path('ud_carnet_inicio') }}">Inicio</a></li>
                            <li class="redli"><a href="{{ path('carnet_homepage') }}">Carnetizacion</a> </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-9">
            {% block content %}
            {% endblock %}
        </div>
    </div>

{% endblock %}